% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_table.R
\name{cluster_table}
\alias{cluster_table}
\title{Build a Publication-Quality Activation Cluster Table}
\usage{
cluster_table(
  vol,
  threshold,
  atlas = NULL,
  stat_type = c("t", "z", "F", "other"),
  df = NULL,
  coord_space = NULL,
  min_cluster_size = 10L,
  local_maxima_dist = 15,
  max_peaks = 3L,
  connectivity = c("26-connect", "18-connect", "6-connect"),
  sort_by = c("size", "peak_stat")
)
}
\arguments{
\item{vol}{A \code{NeuroVol} object containing the statistical map.}

\item{threshold}{Numeric threshold for cluster detection (absolute value).}

\item{atlas}{Optional atlas object (e.g., a \code{volatlas} from neuroatlas)
for anatomical labeling.}

\item{stat_type}{Type of statistic in the volume: \code{"t"}, \code{"z"},
\code{"F"}, or \code{"other"}.}

\item{df}{Degrees of freedom for p-value computation. A single value for
t/z statistics, or a length-2 vector \code{c(df1, df2)} for F statistics.
\code{NULL} omits p-values.}

\item{coord_space}{Character string naming the coordinate space (e.g.,
\code{"MNI152"}, \code{"MNI305"}). If \code{NULL}, inferred from \code{atlas}
or set to \code{"Unknown"}.}

\item{min_cluster_size}{Minimum number of voxels for a cluster to be retained.}

\item{local_maxima_dist}{Minimum distance (mm) between local maxima within a
cluster.}

\item{max_peaks}{Maximum number of sub-peaks reported per cluster.}

\item{connectivity}{Voxel connectivity for cluster detection.}

\item{sort_by}{Sort clusters by \code{"size"} (descending) or
\code{"peak_stat"} (descending absolute value).}
}
\value{
An S3 object of class \code{"cluster_table"} with elements:
  \describe{
    \item{clusters}{A data.frame with one row per cluster.}
    \item{peaks}{A data.frame with sub-peaks (local maxima) per cluster.}
    \item{threshold}{The threshold used.}
    \item{stat_type}{The statistic type.}
    \item{df}{Degrees of freedom.}
    \item{n_clusters}{Number of clusters.}
    \item{coord_space}{Coordinate space label.}
    \item{atlas_name}{Name of atlas used, or \code{NA}.}
  }
}
\description{
Detects suprathreshold clusters in a statistical brain volume and returns
a structured table of cluster-level and peak-level statistics suitable for
publication (NeuroImage / HBM / COBIDAS compliant).
}
